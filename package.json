{
  "name": "micebuild",
  "version": "0.1.0",
  "description": "",
  "main": "dist/lib/index.js",
  "bin": {
    "micebuild": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "test": "tsx tests/test.ts",
    "dev": "vite",
    "build:lib": "tsc --project tsconfig.json",
    "build:cli": "tsc src/cli/index.ts --outDir dist/cli --project tsconfig.json",
    "build:frontend": "tsc -b && vite build",
    "build": "tsc && npm run build:frontend",
    "prepublishOnly": "npm run build"
  },
  "author": "webninjasi",
  "license": "MIT",
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "@rollup/plugin-replace": "^6.0.2",
    "@types/node": "^22.13.5",
    "tsx": "^4.19.3",
    "typescript": "^4.9.4",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "fengari": "^0.1.4",
    "luaparse": "^0.3.1",
    "preact": "^10.26.2"
  }
}
